# MLflow Experiments

This directory contains experimental configurations and assets.

## Usage

Experiments are tracked via MLflow. Access the UI at http://localhost:5000

## Naming Convention

- `exp_sentiment_baseline` - Baseline sentiment model
- `exp_price_lstm` - LSTM price prediction
- `exp_ensemble` - Ensemble models
